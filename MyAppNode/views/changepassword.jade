     
// Basic Jade specification for querying for an actor
// Inherits from layout 
extend layout
// Then we produce a form that triggers /actor once
// the user hits Submit
block content
  p
    | Changing password for #{login}
  p
    | #{error}
  p
    | Please enter details:
    form(method='post', action='/changepassword', name='myForm')
      input(name='login', type='hidden', value='#{login}')
      <br>
      |Current password: 
      input(name='currentpassword', type='password', value='#{password}')
      <br>
      |New Password: 
      input(name='newpassword',type='password', value='')
      <br>
      |Confirm Password: 
      input(name='confirmpassword',type='password', value='')
      <br>
      input(type='submit', value='Submit')
